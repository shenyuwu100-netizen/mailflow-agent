# v0.2 本地 API 集成验证

日期：2026-09-06。

- 85 项测试通过（含模型阶段结构校验、引用检查、检索、重复请求、并发预算、重启、审核与原流程回归）。
- 20 条原有政策合成回归通过；新两阶段模型记录可离线重放。
- 指定 cun.ai 接口，gpt-5.6-luna：实测与复测共 10 次请求，服务商报告 4,213 tokens。
- 最终代表性案例：中文 FAQ 自动模拟发送；中文混合退款请求、英文物流异常转人工并生成引用知识的草稿。
- 初测发现英文摘要语言漂移和检索带入无关文本，收紧语言/类别定义及停用词后重测通过；保留实测记录，不作为生产准确率估计。
- 实际 Chrome：显示模型分析、两份知识引用与待核实事项；编辑退款草稿后批准，记录 approve → send_claimed → simulated_sent。
- 同库重启后预算仍为已用 6 次，重复发送相同请求 ID 未新增模型调用、任务或发送；后续复测累计至 10 次。
- 验证范围为本地 API 集成；没有部署域名或接入真实邮箱。

本机 API 启动器在项目外读取用户指定的凭据到 Windows 进程内存，未将密钥写入代码、数据库或前端。

## v0.1 历史验证


2026-09-06，Python 3.12。基线提交：f7d274f09852d5e5131ca895aaef5d64c128c036。

- 新增回归测试：54 项通过，包括策略、模型契约、网页、并发、重启、知识变更与 5 项旧流程回归。
- 政策合成用例：20/20 符合预设标签；5 条允许自动处理、15 条转人工。
- 模型实测：1 条合成 FAQ 成功，gpt-5.6-luna，报告用量 199 tokens；之前 1 次接口错误未返回用量。没有真实邮件发送。
- Chrome 实际交互：FAQ 自动模拟发送；退款混合意图转人工；人工确认后模拟发送成功。
- 发布包去除旧数据库、缓存、发信脚本与历史文档；包含原许可证和来源标注。

运行：`pip install -e ".[test,legacy]"`，然后 `python -m pytest tests_mailflow` 和 `python -m mailflow.evaluate`。

原小组完整测试套件与生产部署不在此结果中；真实 Graph 发信仅验证了 Mock 契约。合成用例不是生产准确率估计。
